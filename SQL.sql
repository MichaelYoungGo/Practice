# 创建数据表
CREATE TABLE my_table(name VARCHAR(10),
                      age int );
# 创建字段属性非空的表
CREATE TABLE my_null(
id INT NOT NULL ,
name varchar(10),
price DECIMAL,
dealdate DATE
) CHARACTER SET utf8 collation_name;
